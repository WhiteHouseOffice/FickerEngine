# Decisions Log

> One-liners of things we decided and why. Newest on top.

- YYYY-MM-DD: Adopt WebGPU (JS for bootstrap draw; C++ engine step) — fastest visual feedback in CI.
- YYYY-MM-DD: Protect `.github/**` and `scripts/agent/**` with CODEOWNERS.
- YYYY-MM-DD: Use CMake with `-D BUILD_WEB=ON` for Emscripten builds; Puppeteer for headless frames.
